"use strict";
// Thanks to strict 
let hasDriversLicense = false;
const passTest = true;
if(passTest) hasDriversLicense = true;
if(hasDriversLicense) console.log('I can drive :D');
const Math = 3;
console.log(Math);
const interface = 'Audio';
// interface is a reserve word too 
const private = 324;
// this thing shows error 

// const if =234;

// console(if);